# Data fetching
requests==2.32.3

# GCP
google-cloud-storage==2.17.0
google-cloud-bigquery>=3.20.0

# Data processing
pandas==2.1.4
pyarrow==16.1.0

# Env management
python-dotenv==1.0.1

# Airflow (for local script testing only — real Airflow runs via Docker)
apache-airflow==2.9.3

# Testing
pytest==8.3.2
